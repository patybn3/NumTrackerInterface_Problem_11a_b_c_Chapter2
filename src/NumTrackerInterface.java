//interface class
public interface NumTrackerInterface {

    //will perform all the calculations, addition and average
    void add(int calcNum);
    //will get the total of the sum
    int getSum();
    //will get the total number of digits given for the calculations
    int getCount();
    //will get the calculated average.
    double getAverage();
}
